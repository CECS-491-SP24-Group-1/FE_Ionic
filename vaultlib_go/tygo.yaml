packages:
  # keystore.go
  - path: "wraith.me/vaultlib/vaultlib/keystore"
    output_path: "dist/keystore.ts"
    indent: "\t"
    extends: "ExportedGo"
    frontmatter: 'import { ExportedGo } from "./exported_go";'
    type_mappings:
      crypto.PUBKEY_SIZE: 32
      crypto.Privseed: "Uint8Array"
      crypto.Pubkey: "Uint8Array"

  # evault.go & vault.go
  - path: "wraith.me/vaultlib/vaultlib/vault"
    output_path: "dist/vault.ts"
    indent: "\t"
    extends: "ExportedGo"
    frontmatter: |
      import { KeyStore } from "./keystore";
      import { ExportedGo } from "./exported_go";
    type_mappings:
      util.UUID: "string"
      time.Time: "string"
      keystore.KeyStore: "KeyStore"
      sectype.SecType: '"NONE" | "ENC_KEY" | "PASSPHRASE" | "QR" | "BIOMETRICS" | "HW_KEY"'